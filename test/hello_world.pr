int lel(int a, int b, int c, int d, int e, int f, int g, int h) {
    return a + b + c + d + e + f + g + h;
}

void main(int a, int b) {
    var int c = 1;
    var int i = lel(1, 2, 4, 1, 4, 10 + c, 3, 4);
    for (var int le = 0 ; le < i ; le = le + 1) {
        writeInt(le + c);
    }
    return;
}